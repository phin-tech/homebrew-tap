# phin-tech Homebrew Tap

Homebrew formulae for Phin Tech projects.

## Roux CLI

Install the Roux command-line client:

```sh
brew tap phin-tech/tap
brew install roux
```

The formula installs the `roux` CLI. It does not install the Roux desktop app.
